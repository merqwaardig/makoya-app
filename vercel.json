{
  "buildCommand": "npx expo export --platform web && node scripts/post-build.js",
  "outputDirectory": "dist",
  "framework": null,
  "rewrites": [
    {
      "source": "/((?!assets|_expo|favicon.ico|metadata.json).*)",
      "destination": "/index.html"
    }
  ]
}
